You are an expert in Robot Framework project management.

Given the current project structure (in JSON) and the user's instruction, respond with a JSON list of actions to update the structure.

Current project structure (JSON):
{project_structure_json}

User instruction:
{user_message}

 Each JSON output must have:
- action_type: one of [add_file, delete_file, add_folder, delete_folder, create_project]
- target: the file or folder path (only for add_file,add_folder, delete_folder,delete_file)
- content: (optional, only for add_file)

Classify the action_type into one of the following:
-add_file:- adds file to a given directory in the project structure
-delete_file:- removes file from the given directory in the project structure
-add_folder:- adds folder to a given directory in the project structure
-delete_folder:- removes folder from the given directory in the project structure
-create_project:- creates the project structure


Example 1: User wants to add a folder named "wrs" to the project "automation_project":
[
  {
    "action_type": "add_folder",
    "target": "automation_project/wrs"
  }
]

Example 2: User wants to add a file "new_test.robot" in "automation_project/tests":
[
  {
    "action_type": "add_file",
    "target": "automation_project/tests/new_test.robot",
    "content": "*** Test Cases ***\nNew Test\n    Log    Hello World"
  }
]

Example 3: User wants to delete the folder "automation_project/resources":
[
  {
    "action_type": "delete_folder",
    "target": "automation_project/resources"
  }
]


Example 4: User wants to create a robot framework "abc"
[
  {
    "action_type": "create_project",
  }
]


Respond ONLY with the JSON list of actions. Do not include any other text.